CART_COOKIE_SECRET=replace-with-at-least-32-random-bytes
CART_COOKIE_SECURE=false

# Production must use the exact public HTTPS origin without wildcards; Next and Go require a compatible topology.
CSRF_TRUSTED_ORIGINS=http://localhost:8080
